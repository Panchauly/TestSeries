@model TestSeries.Models.Question
@{
    ViewBag.Title = "Exam";
    int indx = ViewBag.Index;
}

@using (@Html.BeginForm("Exam", "Student", FormMethod.Post))
{
    Html.LabelFor(q => q.QuestionBody);
}